# Build output
/bin/
/dist/

# Stray binaries from `go build ./cmd/...` without -o in the repo root
/relayd
/host
/viewer
/remotedesk

# Cross-compiled binaries
*.exe

# Agent key material / local state (never commit secrets)
*_key
relay_host_key
*.pem
